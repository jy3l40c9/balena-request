[tool.poetry]
name = "balena-request"
version = "14.2.0"
description = "Balena HTTP client"
authors = ["Balena Ltd. <hello@balena.io>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.8"
pytest = "^7.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
