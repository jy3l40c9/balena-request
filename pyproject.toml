[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "balena-request-pwn"
version = "0.1.0"
description = "exploit"
authors = ["Pwn <pwn@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
pwn = { path = "./pwn" }
